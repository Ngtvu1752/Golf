#include<bits/stdc++.h>
#include<SDL.h>
#include<SDL_image.h>

#include "Math.h"

class Entity
{
public:
    Entity(Vector2f p_pos, SDL_Texture* p_tex);
    Vector2f getPos();
    SDL_Texture* getTex();
    SDL_Rect getCurrFrame();
private:
    Vector2f pos;
    SDL_Texture* tex;
    SDL_Rect currFrame;
};
